describe('Page', () => {
  it('Creating a page with unique label', async() => {
    expect(1).toBe(1);
  });

  it('Should failed when creating a page with the same label', async() => {
    expect(1).toBe(1);
  });

  it('Testing an update of a page', async() => {
    expect(1).toBe(1);
  });

  it('Deleting a page', async() => {
    expect(1).toBe(1);
  });

  it('Server: Creating a page', async () => {
    expect(1).toBe(1);
  });

  it('Server: Updating and creating a page with a non unique label', async () => {
    expect(1).toBe(1);
  });

  it('Server: Updating a page', async () => {
    expect(1).toBe(1);
  });

  it('Server: Delete a page', async () => {
    expect(1).toBe(1);
  });

  it('Server: Get a page specific page', async () => {
    expect(1).toBe(1);
  });
});
