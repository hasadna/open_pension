describe('Managing body', () => {
  it('Send query when there no managing bodies', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single managing body', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two managing bodies', () => {
    expect(1).toBe(1);
  });
});
