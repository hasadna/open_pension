describe('File info', () => {
  it('Send query when there no files', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single file', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two files', () => {
    expect(1).toBe(1);
  });
});
