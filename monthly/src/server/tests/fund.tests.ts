describe('Funds', () => {
  it('Send query when there no funds', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single fund', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two funds', () => {
    expect(1).toBe(1);
  });
});
