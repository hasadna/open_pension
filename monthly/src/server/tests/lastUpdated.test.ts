describe('Last updated', () => {
  it('Send query when there no records', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single record', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two records', () => {
    expect(1).toBe(1);
  });
});
