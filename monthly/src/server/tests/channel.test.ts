describe('Channel', () => {
  it('Send query when there no channels', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single channel', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two channels', () => {
    expect(1).toBe(1);
  });
});
