describe('Sub channels', () => {
  it('Send query when there no sub channels', () => {
    expect(1).toBe(1);
  });

  it('Send query when there is a single sub channel', () => {
    expect(1).toBe(1);
  });

  it('Send query when there are two sub channels', () => {
    expect(1).toBe(1);
  });
});
