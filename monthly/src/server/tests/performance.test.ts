describe('Performance', () => {

  it('Testing the results when there is no data', () => {
    expect(1).toBe(1);
  });

  it('Verify convertTimePeriodToTimeRangeQuery return the correct time ranges', () => {
    expect(1).toBe(1);
  });

  it('Verify the query return the correct values', () => {
    expect(1).toBe(1);
  });
});
