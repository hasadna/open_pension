version: "3.7"

volumes:
  application-db:
    external: false
  processor-db:
    external: false

services:
  gateway:
    image: hasadna/open-pension-gateway:latest
    restart: always
    build:
      context: ./gateway
      cache_from:
        - hasadna/open-pension-gateway:latest

  application:
    image: hasadna/open-pension-application:latest
    restart: always
    build:
      context: ./application
      cache_from:
        - hasadna/open-pension-application:latest
    depends_on:
      - application_db
    env_file:
      - ./application/.docker.env

  application_db:
    image: mysql:5.7
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: application
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db/data:/var/lib/mysql

  client:
    image: hasadna/open-pension-client:latest
    restart: always
    build:
      context: ./client
      cache_from:
        - hasadna/open-pension-client:latest
    ports:
      - "80:80"
    environment:
      - SERVICE_PORTS=80
    depends_on:
      - gateway

  processor:
    image: hasadna/open-pension-processor:latest
    restart: always
    build:
      context: ./processor
      cache_from:
        - hasadna/open-pension-processor:latest
    env_file:
      - ./processor/.env.docker

    depends_on:
      - processor_db

  processor_db:
    image: mongo
    restart: always
    volumes:
      - processor-db:/data/db

  cms:
    image: hasadna/open-pension-cms:latest
    restart: always
    depends_on:
      - cms_db
    build:
      context: ./cms
      cache_from:
        - hasadna/open-pension-cms:latest
  cms_db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: cms
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./cms-db-data:/var/lib/mysql

  blog:
    image: hasadna/open-pension-blog:latest
    build:
      context: ./blog
      cache_from:
        - hasadna/open-pension-blog:latest
    depends_on:
      - cms
    environment:
      - trigger_deploy_secret_key=pizza
      - GH_ACCESS_TOKEN=hamburger
    restart: "always"
    network_mode: "service:cms"

  kafka:
    image: hasadna/open-pension-kafka:latest
    restart: always
    build:
      context: ./kafka
      cache_from:
        - hasadna/open-pension-kafka:latest
    environment:
      - BROKER_ID=1
    depends_on:
      - zookeeper

  kafka2:
    image: hasadna/open-pension-kafka:latest
    restart: always
    environment:
      - BROKER_ID=2
    depends_on:
      - zookeeper

  zookeeper:
    restart: always
    image: zookeeper:3.5

  fetcher:
    image: hasadna/open-pension-fetcher:latest
    restart: always
    build:
      context: ./fetcher
      cache_from:
        - hasadna/open-pension-fetcher:latest

  storage:
    image: hasadna/open-pension-storage:latest
    restart: always
    build:
      context: ./storage
      cache_from:
        - hasadna/open-pension-storage:latest
    depends_on:
      - storage_db
    env_file:
      - ./storage/.docker.env

  storage_db:
    image: mysql:5.7
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: storage
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./storage/data:/var/lib/mysql

  monthly:
    image: hasadna/open-pension-monthly:latest
    restart: always
    build:
      context: ./monthly
      cache_from:
        - hasadna/open-pension-monthly:latest
    depends_on:
      - monthly_db
    env_file:
      - ./monthly/.docker.env

  monthly_db:
    image: mysql:5.7
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: monthly
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./monthly/data:/var/lib/mysql
